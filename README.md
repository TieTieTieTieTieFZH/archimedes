# Archimedes | 分布式学术智能引擎

## 快速开始

### ⚠️ 重要：环境要求
- JDK 17+
- **请勿安装全局 Maven！** 本项目使用 Maven Wrapper

### 构建和运行

**Windows:**
```cmd
# 构建项目
.\mvnw.cmd clean compile

# 运行测试
.\mvnw.cmd test

# 启动应用
.\mvnw.cmd spring-boot:run
```

**Linux/Mac:**
```bash
# 构建项目
./mvnw clean compile

# 运行测试
./mvnw test

# 启动应用
./mvnw spring-boot:run
```

### 便捷脚本

项目提供了便捷脚本，位于 `scripts/` 目录：

**Windows:**
```cmd
scripts\build.cmd   # 构建项目
scripts\test.cmd    # 运行测试  
scripts\run.cmd     # 启动应用
```

**Linux/Mac:**
```bash
chmod +x scripts/*.sh
scripts/build.sh    # 构建项目
scripts/test.sh     # 运行测试
scripts/run.sh      # 启动应用
```

### 开发环境配置

详细的开发环境配置请参考 [开发环境配置指南](docs/DEVELOPMENT_SETUP.md)

### 技术栈

- Spring Boot 3.5.10
- Spring Data Elasticsearch
- MyBatis Plus
- MySQL
- Knife4j (API 文档)

## 项目架构

### 🏗️ 整体架构
Archimedes 采用经典的**分层架构**设计，专注单体应用的简洁性和可维护性：

```
┌─────────────┬──────────────┬─────────────────┐
│  Embedding  │  Ingestion   │     Search      │
├─────────────┼──────────────┼─────────────────┤
│• 文本→向量   │• 数据清洗     │• 关键词搜索     │
│• 批量处理   │• ETL流水线    │• 语义搜索       │
│• 结果缓存   │• 质量控制     │• 结果分页       │
└─────────────┴──────────────┴─────────────────┘
```

### 📦 包结构设计

```
com.example.archimedes/
├── dto/                      # 📄 统一数据传输对象
│   ├── request/              # 请求DTO (SearchRequest, VectorizeRequest)
│   ├── response/             # 响应DTO (SearchResponse, PaperVO, VectorizeResponse)
│   └── internal/             # 内部流转DTO (RawPaper, ProcessedPaper, IngestionSummary)
├── service/                  # 🔧 业务服务层
│   ├── search/               # 搜索服务接口
│   ├── embedding/            # 向量化服务接口  
│   └── ingestion/            # 数据摄取服务接口
├── controller/               # 🌐 REST API控制器
├── config/                   # ⚙️ Spring配置类
├── entity/                   # 💾 数据库实体映射
├── mapper/                   # 🗃️ 数据访问层 (MyBatis)
├── exception/                # ⚠️ 统一异常处理
├── utils/                    # 🛠️ 工具类和转换器
├── search/                   # 🔍 搜索模块实现
├── embedding/                # 🧠 向量化模块实现
└── ingestion/                # 📥 数据摄取模块实现
```

### 🎯 设计原则

1. **简洁统一** - 经典MVC分层，易于理解和维护
2. **职责分离** - 每个模块有明确的单一职责
3. **类型安全** - 强类型DTO，编译时检查
4. **可扩展性** - 清晰的接口设计为功能扩展提供基础

## 项目结构

```
src/
├── main/
│   ├── java/com/example/archimedes/
│   │   ├── dto/             # 数据传输对象
│   │   │   ├── request/     # 请求DTO
│   │   │   ├── response/    # 响应DTO
│   │   │   └── internal/    # 内部DTO
│   │   ├── service/         # 服务接口层
│   │   ├── controller/      # REST控制器
│   │   ├── config/          # 配置类
│   │   ├── entity/          # 数据库实体
│   │   ├── mapper/          # 数据访问层
│   │   ├── exception/       # 异常定义
│   │   ├── utils/           # 工具类
│   │   ├── search/          # 搜索模块
│   │   ├── embedding/       # 向量化模块
│   │   └── ingestion/       # 数据摄取模块
│   └── resources/
│       ├── application.yaml # 配置文件
│       └── db/              # 数据库脚本
└── test/                    # 测试代码

## API 文档

启动应用后访问：http://localhost:8080/doc.html

## 贡献指南

1. 使用项目提供的 Maven Wrapper
2. 遵循代码规范
<<<<<<< HEAD
3. 确保测试通过后再提交代码
=======
3. 确保测试通过后再提交代码
>>>>>>> 6f7e651ddd1b92b1b202780f0c0090db3cdd14b0
